## Packages
date-fns | Formatting timestamps (e.g., "HH:mm:ss")
lucide-react | Icons for the dashboard (already in base, but emphasizing usage)
clsx | Conditional class names (already in base)
tailwind-merge | Merging tailwind classes (already in base)

## Notes
WebSocket connection required at /ws for real-time updates.
Protocol includes INIT_STATE, UPDATE_TERMINAL, NEW_CALL events.
Theme: Cyberpunk industrial, high contrast, dark mode by default.
Font: Monospace for data, Sans for UI.
